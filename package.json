{
  "name": "jmz-data-editor",
  "private": true,
  "license": "MIT",
  "scripts": {
    "start": "distrobox enter ex -- neu run -- --window-enable-inspector",
    "dev": "distrobox enter ex -- neu run -- --window-enable-inspector",
    "build": "distrobox enter ex -- neu build",
    "publish": "distrobox enter ex -- neu build --release"
  },
  "devDependencies": {
    "@types/node": "^22.9.3"
  }
}

enum CraftingListType
{
  Ingredients = 'Ingredients',
  Tools = 'Tools',
  Outputs = 'Outputs',

  // what a shop charges to teach the recipe, paid once. the value doubles as the card heading.
  Cost = 'Cost',
}

export default CraftingListType;